apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
    name: kafka-manager
    namespace: default
spec:
    chart:
        repository: https://kubernetes-charts.storage.googleapis.com
        name: kafka-manager
        version: 2.3.2
